---
AWSTemplateFormatVersion: "2010-09-09"
Description: "will.deberry IAM Role via CFN"

Resources:
  IAMRole:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Sub '${AWS::AccountId}'
            Action:
              - sts:AssumeRole
      Description: will.deberry iam role
      RoleName: will.deberry-iam-role

  IAMRole2:
    Type: AWS::IAM::Role
    Properties:
      AssumeRolePolicyDocument:
        Version: "2012-10-17"
        Statement:
          - Effect: Allow
            Principal:
              AWS: !Sub '${AWS::AccountId}'
            Action:
              - sts:AssumeRole
      Description: will.deberry iam role2
      RoleName: will.deberry-iam-role2

  IAMPolicy:
    Type: AWS::IAM::ManagedPolicy
    Properties:
      Description: will.deberry-iam-read-only-managed re-used
      ManagedPolicyName: will.deberry-iam-policy-re-used
      Roles:
        - !Ref IAMRole
        - !Ref IAMRole2
      PolicyDocument:
        Version: '2012-10-17'
        Statement:
          - Effect: Allow
            Action:
              - iam:GenerateCredentialReport
              - iam:GenerateServiceLastAccessedDetails
              - iam:Get*
              - iam:List*
              - iam:SimulateCustomPolicy
              - iam:SimulatePrincipalPolicy
            Resource: '*'
